fastapi==0.115.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.0

# Для работы с запросами к внешним API (например, к gen-api.ru)
requests==2.32.3

# Опционально: для логирования, тестов, CORS
httpx==0.27.2
uvicorn[standard]  # уже есть, но убедимся, что включён стандартный набор (reload, etc.)
fastapi-cli==0.0.5

# Для обработки даты/времени (если нужно)
python-dateutil==2.9.0
fastapi-utils
# Для работы с UUID, если генерируете имена файлов